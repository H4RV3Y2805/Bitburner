import { NS } from '@ns'

export async function main(ns : NS) : Promise<void> {
    
    // Do I want to focus while creating programs?
    // Yes if I don't have the augmentation, no otherwise.
    const focus = Number(ns.args[0]);

    // Create programs in this order:
    // Autolink.exe
    // 

}